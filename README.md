# Databases-Travel-Agency
Designed and implemented a relational database for a travel agency using MySQL, including schema normalization, constraints, indexes, stored procedures, triggers, and query optimization. The project extends the data model with new entities while enforcing business rules and maintaining data integrity.
